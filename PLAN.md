# 中文 CV 双版本重做计划

## 目标

在 `cv/zh-arch` 与 `cv/zh-ic` 分支分别维护两页 A4 中文简历。Architecture 版突出体系结构、
专用加速器和软硬件协同；IC Design 版突出数字逻辑、RTL、验证与实现。

博士预计毕业时间统一为 **2026 年 11 月**。

## 内容原则

- Thesis 只用于核实事实，不决定简历的技术颗粒度。
- 保留 ASOCA3、ASOCA2、ReMap、ASOCat、CoNM 等对外项目名，删除内部模块、指令集和工具系统名称。
- 尽量使用中文；只保留 RTL、FPGA、ASIC、RISC-V、SystemVerilog、UVM、NoC、PCIe、工具名、学校名和论文正式标题等必要英文。
- 联系方式下只放一行岗位定位，不设置独立“技术概述”章节。
- 不写贡献边界免责声明；只保留能够准确归属于本人的工作。
- 核心项目每项 2--3 条，补充项目每项 1--2 条，每条尽量不超过两行。
- 每版最多保留两组有辨识度的量化成果。
- 若版面不足，依次补充：ReMap、半导体缺陷检测、ASOCat、CoNM、分层存储。

## Architecture 版

核心顺序：ASOCA3、FPGA 双可寻址存储器、ASOCA2、ASOCA 开发与验证基础设施。

- ASOCA3：加速器体系结构、定制指令集、流水处理核、存储和片上互联接口。
- FPGA 双可寻址存储器：存储组织、双模式访问、资源效率和扩展性。
- ASOCA2：近存计算架构、专用存储器、ASIC 原型和流片经历。
- 开发基础设施：自动化流程、工作负载生成、性能对比与体系结构评估。
- 技能分为“体系结构、硬件设计、工具与编程”；论文保留三篇，不添加技术标签。

## IC Design 版

核心顺序：ASOCA2、ASOCA3、FPGA 双可寻址存储器、ASOCA 开发与验证基础设施。

- ASOCA2：数字逻辑、控制与数据通路、顶层验证、综合流片和芯片测试。
- ASOCA3：处理核 RTL、多级流水、控制逻辑、存储接口和 FPGA 验证。
- FPGA 双可寻址存储器：参数化 RTL、片上存储映射、查找与读写控制。
- 验证基础设施：自动化流程、参考模型、专用验证环境和结果检查。
- 技能分为“数字设计、功能验证、实现与工具、编程”；论文保留最相关两篇。

## 排版

- A4，左右页边距 17 mm，上边距 14 mm，下边距 15 mm。
- 有效正文 11pt，行距不低于 1.0；不以缩小字号或极窄页边距填页。
- 删除手工 `\newpage`，依靠内容数量和段落间距自然分页。
- 通过防孤行控制避免章节标题或项目标题单独落在页尾。
- 若第二页末尾距页面底部超过约 35 mm，按既定优先级增加补充项目，达到舒适填充后停止。

## 真实性与 Git 安全

- 不推测未能确认的工具、流程、协议或性能数据。
- ASOCA3 写作 FPGA 加速系统，不称为 SoC。
- ASOCA2 可写流片与芯片测试，不写成功完成硅后功能验证。
- `thesis/` 是只读嵌套仓库；禁止暂存或修改。
- 在现有两个分支追加纠正提交，不改写历史。

## 验收

- 两版均为恰好两页 A4 PDF。
- 无 overfull、裁切、孤立标题、异常换行或乱码。
- 页面视觉密度均衡，页底不存在明显人为留白。
- PDF 文字可提取，字体完整嵌入。
- Architecture 与 IC Design 的项目顺序、表述重点、技能分类和论文权重明显不同。
